# 第二轮测试修复 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修掉第二轮人工测试发现的问题：恢复活动列表草稿「编辑」入口、AI 助理消息乐观渲染、放松生成类 prompt 并补足后台助理/飞书 Bot 输入上下文、按 Pine 还原基础控件并补齐移动端响应式。

**Architecture:** pnpm monorepo。`@teampilot/server`（Fastify + Prisma + 豆包 LLM，vitest + fake LLM 捕获 `(system,user)` prompt）；`@teampilot/web-admin`（React + Tailwind「Pine」+ react-router，vitest + testing-library）。新增的 AI 上下文辅助集中放 `server/src/ai/context.ts` 复用；新增 UI 控件集中放 `web-admin/src/components/Controls.tsx`。全程 TDD：先写/改测试 → 跑红 → 实现 → 跑绿 → 提交。

**Tech Stack:** TypeScript, React 18, Tailwind 3.4, react-router 6, Fastify, Prisma, vitest, @testing-library/react, zod。

依据 spec：`docs/superpowers/specs/2026-06-03-第二轮测试修复-design.md`

---

## 文件结构

**新建：**
- `packages/web-admin/src/components/Controls.tsx` — 可复用 `Radio` / `Checkbox`（隐藏原生 input + Pine 样式）。
- `packages/web-admin/src/components/MobileNav.tsx` — 移动端底部固定 Tab 栏。
- `packages/web-admin/test/controls.test.tsx` — 控件单测。

**修改：**
- `packages/web-admin/src/pages/Activities.tsx` — 草稿「编辑」入口 + 移动端卡片列表。
- `packages/web-admin/src/pages/Assistant.tsx` — `send()` 乐观渲染 + 聊天容器移动端样式。
- `packages/web-admin/src/pages/ActivityForm.tsx` — 用 `Radio`/`Checkbox` 替换原生控件 + 操作按钮移动端纵向。
- `packages/web-admin/src/pages/ActivityDetail.tsx` — Banner 移动端纵向。
- `packages/web-admin/src/components/Layout.tsx` — 侧栏移动端隐藏 + 挂载 `MobileNav` + 主区留白。
- `packages/web-admin/src/components/icons.tsx` — 新增 `Clock` / `MapPin`。
- `packages/web-admin/src/index.css` — `.doc-h` 竖杠对齐修复。
- `packages/server/src/ai/context.ts` — `OUTPUT_GUARD` 收敛 + 新增 `positionBreakdown` / `futurePublishedActivities` / `recentEndedDetailed` / `memberAttendanceStats`。
- `packages/server/src/ai/scenarios.ts` — 四个生成场景 system prompt。
- `packages/server/src/assistant/service.ts` — 助理 system + user prompt 补全。
- `packages/server/src/ai/memberBot.ts` — Bot system + user prompt 补全（公开报名名单）。
- 对应测试：`activities.test.tsx`、`assistant.test.tsx`(web)、`scenarios.test.ts`、`assistant.test.ts`(server)、`memberBot.test.ts`、`aiContext.test.ts`。

**测试命令速查：**
- 单文件（server）：`pnpm --filter @teampilot/server exec vitest run test/<file>.test.ts`
- 单文件（web）：`pnpm --filter @teampilot/web-admin exec vitest run test/<file>.test.tsx`
- 全量：`pnpm --filter @teampilot/server test` / `pnpm --filter @teampilot/web-admin test`

> **行号约定：** 各步骤给出的行号是写计划时的快照提示，仅供定位；以「替换 old → new」展示的代码内容为准（Edit 按内容匹配）。同一文件被多个 task 修改时，按 task 顺序执行，后续行号会自然漂移。

---

## Task 1：活动列表草稿「编辑」入口（TP2-ACT-01）

**Files:**
- Modify: `packages/web-admin/src/pages/Activities.tsx:50-52`
- Test: `packages/web-admin/test/activities.test.tsx`

- [ ] **Step 1: 写失败测试**

在 `packages/web-admin/test/activities.test.tsx` 的 `describe("Activities", …)` 内、现有 `it` 之后追加：

```tsx
  it("shows 编辑 only for draft activities", async () => {
    (globalThis.fetch as any).mockResolvedValue({ ok: true, status: 200, json: async () => [
      { id: "d1", name: "草稿活动", type: "training", startTime: "2026-06-05T10:00:00.000Z", location: "x", status: "draft", attendanceSummary: "—", reviewStatus: "无记录" },
      { id: "p1", name: "已发布活动", type: "match", startTime: "2026-06-06T10:00:00.000Z", location: "y", status: "published", attendanceSummary: "1 去 ｜ 0 不去 ｜ 0 未反馈", reviewStatus: "无记录" },
    ] } as Response);
    render(<MemoryRouter><Activities /></MemoryRouter>);
    const editLinks = await screen.findAllByRole("link", { name: "编辑" });
    expect(editLinks).toHaveLength(1);
    expect(editLinks[0]).toHaveAttribute("href", "/activities/d1/edit");
    expect(screen.getAllByRole("link", { name: "详情" })).toHaveLength(2);
  });
```

- [ ] **Step 2: 跑测试确认失败**

Run: `pnpm --filter @teampilot/web-admin exec vitest run test/activities.test.tsx`
Expected: FAIL —— `Unable to find role="link" name="编辑"`（当前操作列只有「详情」）。

- [ ] **Step 3: 实现操作列**

把 `packages/web-admin/src/pages/Activities.tsx` 的操作列单元格（当前第 50-52 行）：

```tsx
                <td className="col-action">
                  <Link className="text-sm font-medium text-brand transition-colors hover:text-brand-hover" to={`/activities/${a.id}`}>详情</Link>
                </td>
```

改为：

```tsx
                <td className="col-action">
                  <div className="flex items-center gap-3">
                    {a.status === "draft" && (
                      <Link className="text-sm font-medium text-brand transition-colors hover:text-brand-hover" to={`/activities/${a.id}/edit`}>编辑</Link>
                    )}
                    <Link className="text-sm font-medium text-brand transition-colors hover:text-brand-hover" to={`/activities/${a.id}`}>详情</Link>
                  </div>
                </td>
```

- [ ] **Step 4: 跑测试确认通过**

Run: `pnpm --filter @teampilot/web-admin exec vitest run test/activities.test.tsx`
Expected: PASS（含原有「renders rows」用例）。

- [ ] **Step 5: 提交**

```bash
git add packages/web-admin/src/pages/Activities.tsx packages/web-admin/test/activities.test.tsx
git commit -m "$(printf 'feat(web-admin): 活动列表草稿活动恢复编辑入口 (TP2-ACT-01)\n\nCo-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>')"
```

---

## Task 2：AI 队长助理消息乐观渲染（TP2-ASST-01）

**Files:**
- Modify: `packages/web-admin/src/pages/Assistant.tsx:22-33`
- Test: `packages/web-admin/test/assistant.test.tsx`

- [ ] **Step 1: 写失败测试**

在 `packages/web-admin/test/assistant.test.tsx` 的 `describe("Assistant", …)` 内追加：

```tsx
  it("shows the user's message immediately while the request is in flight", async () => {
    (globalThis.fetch as any).mockImplementation(async (url: any) => {
      if (String(url).endsWith("/messages")) return { ok:true, status:200, json: async()=>[] } as Response;
      if (String(url).endsWith("/ask")) return await new Promise<Response>(()=>{}); // never resolves
      return { ok:true, status:200, json: async()=>({}) } as Response;
    });
    render(<Assistant />);
    await userEvent.type(screen.getByLabelText("提问输入"), "立即显示的问题");
    await userEvent.click(screen.getByRole("button", { name: "发送" }));
    expect(await screen.findByText("立即显示的问题")).toBeInTheDocument();
  });
  it("does not duplicate the user message after the request resolves", async () => {
    render(<Assistant />);
    await userEvent.type(screen.getByLabelText("提问输入"), "唯一问题");
    await userEvent.click(screen.getByRole("button", { name: "发送" }));
    expect(await screen.findByText(/近一月训练稳定/)).toBeInTheDocument();
    expect(screen.getAllByText("唯一问题")).toHaveLength(1);
  });
```

- [ ] **Step 2: 跑测试确认失败**

Run: `pnpm --filter @teampilot/web-admin exec vitest run test/assistant.test.tsx`
Expected: 「shows the user's message immediately」FAIL —— 当前用户消息在 `await` 之后才追加，请求未完成时找不到「立即显示的问题」。

- [ ] **Step 3: 实现乐观渲染**

把 `packages/web-admin/src/pages/Assistant.tsx` 的 `send()`（当前第 22-33 行）整体替换为：

```tsx
  async function send(question?: string) {
    const text = (question ?? q).trim();
    if (!text || busyRef.current) return;
    busyRef.current = true;
    setBusy(true);
    setQ("");
    const askedAt = new Date().toISOString();
    setMsgs((prev) => [...prev, { role: "captain", content: text, createdAt: askedAt }]);
    try {
      const r = await post<object>("/api/admin/assistant/ask", { question: text });
      setMsgs((prev) => [...prev, { role: "ai", content: JSON.stringify(r), createdAt: new Date().toISOString() }]);
    } catch {
      setMsgs((prev) => [...prev, { role: "ai", content: "⚠️ 生成失败，请稍后重试", createdAt: new Date().toISOString() }]);
    } finally {
      busyRef.current = false; setBusy(false);
    }
  }
```

说明：`busyRef` 守卫仍在 `await` 之前（双提交防护不变）；用户消息进入后立即追加；成功后只追加 AI；失败保留用户消息并追加纯文本错误气泡（`renderContent` 对非 JSON 原样返回）。

- [ ] **Step 4: 跑测试确认通过**

Run: `pnpm --filter @teampilot/web-admin exec vitest run test/assistant.test.tsx`
Expected: PASS（含原有「sends a question」「example」「Enter」「does not double-submit」全部用例）。

- [ ] **Step 5: 提交**

```bash
git add packages/web-admin/src/pages/Assistant.tsx packages/web-admin/test/assistant.test.tsx
git commit -m "$(printf 'feat(web-admin): AI 助理发送后乐观渲染用户消息 (TP2-ASST-01)\n\nCo-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>')"
```

---

## Task 3：全局 OUTPUT_GUARD 收敛 + 四生成场景 system prompt

**Files:**
- Modify: `packages/server/src/ai/context.ts:7`
- Modify: `packages/server/src/ai/scenarios.ts:24,37,52,68`
- Test: `packages/server/test/scenarios.test.ts`

- [ ] **Step 1: 写失败测试**

在 `packages/server/test/scenarios.test.ts` 的 `describe("scenarios", …)` 内追加两条（队长规则进 prompt）：

```ts
  it("training advice prompt includes captain training rules", async () => {
    const a = await actWithParticipants("training", 1);
    await prisma.teamSettings.upsert({ where: { id: "singleton" }, create: { id: "singleton", defaultLocation: "主场", trainingRules: "每次先热身10分钟" }, update: { trainingRules: "每次先热身10分钟" } });
    const llm = fakeLLM({ goal: "目标".repeat(30), plan: "安排".repeat(60) });
    await generateTrainingAdvice(a.id, llm, new Date());
    const [, user] = (llm.completeJSON as any).mock.calls.at(-1);
    expect(user).toContain("每次先热身10分钟");
  });
  it("match advice prompt includes captain match rules", async () => {
    const a = await actWithParticipants("match", 1);
    await prisma.teamSettings.upsert({ where: { id: "singleton" }, create: { id: "singleton", defaultLocation: "主场", matchRules: "首发稳守反击" }, update: { matchRules: "首发稳守反击" } });
    const llm = fakeLLM({ strategy: "策略".repeat(30), starting: "首发".repeat(30), bench: "替补".repeat(30) });
    await generateMatchAdvice(a.id, llm, new Date());
    const [, user] = (llm.completeJSON as any).mock.calls.at(-1);
    expect(user).toContain("首发稳守反击");
  });
```

- [ ] **Step 2: 跑测试确认失败/通过基线**

Run: `pnpm --filter @teampilot/server exec vitest run test/scenarios.test.ts`
Expected: 新增两条 PASS（user prompt 已含规则）；如已 PASS 则继续；现有用例仍 PASS。本步骤主要锁定基线，下一步改 prompt 后须保持全绿。

- [ ] **Step 3: 收敛 OUTPUT_GUARD**

把 `packages/server/src/ai/context.ts` 第 7 行 `OUTPUT_GUARD` 替换为：

```ts
export const OUTPUT_GUARD = "【输出格式要求】只返回 JSON 对象，不要输出 Markdown 代码块、解释文字、自然语言前缀或后缀；字段名必须严格匹配 schema，不新增、不改名、不漏字段。";
```

- [ ] **Step 4: 替换四个生成场景 system prompt**

在 `packages/server/src/ai/scenarios.ts`：

`generateTrainingAdvice` 的 `system`（第 24 行）改为：

```ts
  const system = "你是藤球队的训练助理。基于给定数据生成本次训练建议，优先参考队长训练规则，同时参考各参加队员信息、本次活动主题与注意事项、近 2 月历史活动摘要。缺少关键信息时先说明缺少什么，再基于已有信息给出保守建议；不得编造具体活动、队员、出勤事实。用建议式语气（建议/可考虑），不要写成强制命令。不得安排未出现在参加人员名单中的队员。输出 JSON：{goal, plan}。goal 50-150字，plan 100-400字。" + OUTPUT_GUARD;
```

`generateMatchAdvice` 的 `system`（第 37 行）改为：

```ts
  const system = "你是藤球队的比赛助理。基于给定数据生成本次比赛建议，优先参考队长比赛规则，同时参考各参加队员信息、本次活动主题与注意事项（可能包含对手、赛制、场地风险）、近 2 月历史活动摘要。缺少对手信息或赛制信息时先说明不足，再给出保守策略建议；不得编造具体活动、队员、出勤事实。用“建议/可考虑”措辞，不强制阵容。不得把未出现在参加人员名单中的队员排入首发或替补。输出 JSON：{strategy(50-200字), starting(50-200字), bench(50-150字)}。" + OUTPUT_GUARD;
```

`generateReviewSummary` 的 `system`（第 52 行）改为（保留最小公开约束）：

```ts
  const system = `你是藤球队复盘助理。基于给定数据生成本次活动的结构化复盘，优先参考“我的复盘记录”，同时参考本次活动的主题与注意事项、实际到场队员名单、近 2 月历史活动摘要。不编造，不评价未到场队员；信息不足时写“信息不足，暂无法整理。”。复盘摘要面向全员公开，不要逐字输出我的复盘记录原文或队长备注原文。输出 JSON：${fields}。` + OUTPUT_GUARD;
```

`generateActivitySummary` 的 `system`（第 68 行）改为：

```ts
  const system = "你是藤球队记录助理。基于给定数据生成面向全员的活动精简总结。生成时机为 initial 时，只根据活动主题、注意事项、参加人数描述已确认的活动安排与参与范围，不预判训练/比赛效果；生成时机为 post_review 时，优先参考 AI 复盘总结，并结合活动主题、注意事项、参加人数与实际到场人数，追加本次活动的核心公开要点。输出 JSON：{summary}（50-500字）。" + OUTPUT_GUARD;
```

> user prompt 不动（已含名称/类型/时间/时长/地点/主题/注意事项/参加名单/近2月摘要/队长规则；复盘到场队员保持 `memberLine(m, false)`）。schema 不动。

- [ ] **Step 5: 跑测试确认通过**

Run: `pnpm --filter @teampilot/server exec vitest run test/scenarios.test.ts`
Expected: PASS。现有断言「只返回 JSON 对象」「不得把未出现在参加人员名单中的队员排入首发或替补」「活动类型：比赛/训练」「如果无人反馈」「记得带护具」「initial 不含 AI复盘总结」均仍命中；新增两条规则断言通过。

- [ ] **Step 6: 提交**

```bash
git add packages/server/src/ai/context.ts packages/server/src/ai/scenarios.ts packages/server/test/scenarios.test.ts
git commit -m "$(printf 'feat(server): OUTPUT_GUARD 收敛为纯格式约束 + 四生成场景 system prompt 放松边界\n\nCo-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>')"
```

---

## Task 4：后台 AI 队长助理上下文补全（§4）

**Files:**
- Modify: `packages/server/src/ai/context.ts`（新增辅助）
- Modify: `packages/server/src/assistant/service.ts`
- Test: `packages/server/test/aiContext.test.ts`、`packages/server/test/assistant.test.ts`

- [ ] **Step 1: 写辅助函数失败测试**

在 `packages/server/test/aiContext.test.ts` 末尾、`describe("recentSummaries", …)` 之后追加：

```ts
import { positionBreakdown, memberAttendanceStats } from "../src/ai/context.js";

describe("positionBreakdown", () => {
  it("counts active members by primary position with Chinese labels", () => {
    const out = positionBreakdown([
      { primaryPosition: "tekong" }, { primaryPosition: "tekong" }, { primaryPosition: "feeder" }, { primaryPosition: "striker" },
    ]);
    expect(out).toBe("发球手 2 人，二传手 1 人，攻球手 1 人");
  });
});

describe("memberAttendanceStats", () => {
  it("computes present rate over ended activities within window", async () => {
    const m = await prisma.member.create({ data: { name: "甲", primaryPosition: "tekong", status: "active", feishuOpenId: "ou_stat" } });
    const a = await prisma.activity.create({ data: { name: "上次训练", type: "training", status: "ended", location: "x", startTime: new Date(Date.now() - 3 * DAY) } });
    await prisma.activityParticipant.create({ data: { activityId: a.id, memberId: m.id, attendanceResponse: "going", actualAttendance: "present" } });
    const stats = await memberAttendanceStats(new Date(), 30);
    const s = stats.find((x) => x.name === "甲")!;
    expect(s.going).toBe(1);
    expect(s.present).toBe(1);
    expect(s.rate).toBe(100);
  });
});
```

> `DAY` 常量已在该文件顶部定义（`const DAY = 86400000;`）。

- [ ] **Step 2: 跑测试确认失败**

Run: `pnpm --filter @teampilot/server exec vitest run test/aiContext.test.ts`
Expected: FAIL —— `positionBreakdown`/`memberAttendanceStats` 未导出。

- [ ] **Step 3: 在 context.ts 新增辅助**

在 `packages/server/src/ai/context.ts` 末尾追加（`TWO_MONTHS`、`prisma`、`positionLabel/levelLabel` 已在文件顶部 import）：

```ts
const DAY = 86400000;

export function positionBreakdown(members: { primaryPosition: string }[]) {
  const c: Record<string, number> = { tekong: 0, feeder: 0, striker: 0 };
  for (const m of members) if (m.primaryPosition in c) c[m.primaryPosition]++;
  return `发球手 ${c.tekong} 人，二传手 ${c.feeder} 人，攻球手 ${c.striker} 人`;
}

export function futurePublishedActivities(now: Date, takeN = 5, days = 30) {
  const until = new Date(now.getTime() + days * DAY);
  return prisma.activity.findMany({
    where: { status: "published", startTime: { gte: now, lte: until } },
    include: { participants: { include: { member: true } } },
    orderBy: { startTime: "asc" },
    take: takeN,
  });
}

export function recentEndedDetailed(now: Date, takeN = 8) {
  const since = new Date(now.getTime() - TWO_MONTHS);
  return prisma.activity.findMany({
    where: { status: "ended", startTime: { gte: since } },
    include: { review: true },
    orderBy: { startTime: "desc" },
    take: takeN,
  });
}

export type AttendanceStat = { name: string; going: number; noResponse: number; present: number; absent: number; rate: number };

export async function memberAttendanceStats(now: Date, days: number): Promise<AttendanceStat[]> {
  const since = new Date(now.getTime() - days * DAY);
  const members = await prisma.member.findMany({ where: { status: "active" }, orderBy: { createdAt: "asc" } });
  const acts = await prisma.activity.findMany({
    where: { startTime: { gte: since }, status: { in: ["published", "ended"] } },
    include: { participants: true },
  });
  return members.map((m) => {
    let going = 0, noResponse = 0, present = 0, absent = 0, endedCount = 0;
    for (const a of acts) {
      const p = a.participants.find((x) => x.memberId === m.id);
      if (!p) continue;
      if (p.attendanceResponse === "going") going++;
      if (p.attendanceResponse === "no_response") noResponse++;
      if (a.status === "ended") { endedCount++; if (p.actualAttendance === "present") present++; if (p.actualAttendance === "absent") absent++; }
    }
    const rate = endedCount ? Math.round((present / endedCount) * 100) : 0;
    return { name: m.name, going, noResponse, present, absent, rate };
  });
}
```

- [ ] **Step 4: 跑辅助测试确认通过**

Run: `pnpm --filter @teampilot/server exec vitest run test/aiContext.test.ts`
Expected: PASS。

- [ ] **Step 5: 写助理 prompt 失败测试（更新旧断言 + 新增上下文断言）**

在 `packages/server/test/assistant.test.ts` 中，把现有 `it("ask system prompt declares 当前判断/判断依据 and data-only constraint", …)`（第 24-32 行）整段**替换**为：

```ts
  it("ask system prompt declares analysis-assistant role and JSON guard", async () => {
    const completeJSON = vi.fn().mockResolvedValue(JSON.stringify({ judgment: "判断".repeat(20), basis: "依据".repeat(20) }));
    await ask("近一月？", { completeJSON } as any, new Date());
    const [system] = (completeJSON as any).mock.calls.at(-1);
    expect(system).toContain("球队管理与训练分析助理");
    expect(system).toContain("judgment");
    expect(system).toContain("只返回 JSON 对象");
  });
  it("ask user prompt carries rules, future activities w/ signup, position & attendance stats", async () => {
    await prisma.teamSettings.upsert({ where: { id: "singleton" }, create: { id: "singleton", defaultLocation: "主场", trainingRules: "先热身", matchRules: "稳守" }, update: { trainingRules: "先热身", matchRules: "稳守" } });
    const m = await prisma.member.create({ data: { name: "张三", primaryPosition: "tekong", status: "active", feishuOpenId: "ou_z" } });
    await prisma.activity.create({ data: { name: "下周训练", type: "training", status: "published", location: "主场", startTime: new Date(Date.now() + 3 * 86400000), durationMinutes: 90, participants: { create: [{ memberId: m.id, attendanceResponse: "going" }] } } });
    const completeJSON = vi.fn().mockResolvedValue(JSON.stringify({ judgment: "判断".repeat(20), basis: "依据".repeat(20) }));
    await ask("下一场有多少人去？", { completeJSON } as any, new Date());
    const [, user] = (completeJSON as any).mock.calls.at(-1);
    expect(user).toContain("先热身");
    expect(user).toContain("稳守");
    expect(user).toContain("下周训练");
    expect(user).toContain("已反馈去:张三");
    expect(user).toContain("发球手");
    expect(user).toContain("出勤率");
  });
```

- [ ] **Step 6: 跑测试确认失败**

Run: `pnpm --filter @teampilot/server exec vitest run test/assistant.test.ts`
Expected: FAIL —— 旧 prompt 不含「球队管理与训练分析助理」「已反馈去:张三」「出勤率」等。

- [ ] **Step 7: 重写 assistant/service.ts**

把 `packages/server/src/assistant/service.ts` 第 3 行 import 改为（去掉未用的 `recentSummaries`/`historyBlock`，加入新辅助）：

```ts
import { activeMembersForAI, memberLine, positionBreakdown, futurePublishedActivities, recentEndedDetailed, memberAttendanceStats, OUTPUT_GUARD, type AttendanceStat } from "../ai/context.js";
```

把 `ask(...)` 函数（第 12-28 行）整体替换为：

```ts
function futureBlock(acts: Awaited<ReturnType<typeof futurePublishedActivities>>) {
  if (!acts.length) return "（暂无未来已发布活动）";
  return acts.map((a) => {
    const going = a.participants.filter((p) => p.attendanceResponse === "going");
    const not = a.participants.filter((p) => p.attendanceResponse === "not_going").length;
    const no = a.participants.filter((p) => p.attendanceResponse === "no_response");
    const goNames = going.map((p) => p.member.name).join("、") || "无";
    const noNames = no.map((p) => p.member.name).join("、") || "无";
    return `- ${a.name}（${a.type === "training" ? "训练" : "比赛"}） / ${a.startTime.toLocaleString("zh-CN", { timeZone: "Asia/Shanghai" })} / ${a.durationMinutes}分钟 / ${a.location} / 主题:${a.theme ?? "无"} / 注意:${a.notes ?? "无"} / 去${going.length}·不去${not}·未反馈${no.length} / 已反馈去:${goNames} / 未反馈:${noNames}`;
  }).join("\n");
}

function endedBlock(acts: Awaited<ReturnType<typeof recentEndedDetailed>>) {
  if (!acts.length) return "（暂无近2月已结束活动）";
  return acts.map((a) => `- ${a.startTime.toLocaleDateString("zh-CN", { timeZone: "Asia/Shanghai" })} ${a.name}（${a.type === "training" ? "训练" : "比赛"}） 主题:${a.theme ?? "无"} 注意:${a.notes ?? "无"} 摘要:${a.review?.aiSummary ?? a.summary ?? "暂无"}`).join("\n");
}

function statsBlock(stats: AttendanceStat[]) {
  if (!stats.length) return "（暂无）";
  return stats.map((s) => `${s.name}：反馈去${s.going}·未反馈${s.noResponse}·实到${s.present}·缺席${s.absent}·出勤率${s.rate}%`).join("\n");
}

export async function ask(question: string, llm: LLMClient, now: Date) {
  const history = await sessionContext(now);
  const members = await activeMembersForAI();
  const settings = await prisma.teamSettings.findUnique({ where: { id: "singleton" } });
  const future = await futurePublishedActivities(now);
  const ended = await recentEndedDetailed(now);
  const stats30 = await memberAttendanceStats(now, 30);
  const stats60 = await memberAttendanceStats(now, 60);

  const system = "你是队长的球队管理与训练分析助理。你可以帮助队长分析球队管理、训练安排、比赛策略、队员资料、阵容搭配、出勤趋势、复盘总结、长期改进和队长规则沉淀相关问题。请优先使用系统提供的队长规则、队员资料、活动安排、出勤记录和复盘摘要；数据不足时明确说明缺少什么，并基于已有信息给出保守、可执行的建议。不要虚构具体活动、队员、出勤或复盘事实。输出 JSON：{judgment(50-300字), basis(50-200字)}。" + OUTPUT_GUARD;

  const convo = history.map((m) => `${m.role === "ai" ? "AI" : "我"}：${m.content}`).join("\n");
  const user = [
    `【当前日期】\n${now.toLocaleString("zh-CN", { timeZone: "Asia/Shanghai" })}`,
    `【对话上下文(10分钟内)】\n${convo || "（新会话）"}`,
    `【当前问题】\n${question}`,
    `【队长规则】\n训练规则：${settings?.trainingRules?.trim() || "暂无"}\n比赛规则：${settings?.matchRules?.trim() || "暂无"}\n默认地点：${settings?.defaultLocation || "暂无"}`,
    `【队员资料】（姓名、球衣号、主位、备位、水平、风格、队长备注）\n${members.map((m) => memberLine(m)).join("\n") || "（无）"}`,
    `【队伍位置概况】\n${positionBreakdown(members)}`,
    `【未来已发布活动】\n${futureBlock(future)}`,
    `【近2月活动摘要】\n${endedBlock(ended)}`,
    `【近30天队员出勤统计】\n${statsBlock(stats30)}`,
    `【近60天队员出勤统计】\n${statsBlock(stats60)}`,
  ].join("\n\n");

  const raw = await llm.completeJSON(system, user);
  const parsed = zAssistant.parse(JSON.parse(raw));
  await prisma.$transaction([
    prisma.assistantMessage.create({ data: { role: "captain", content: question, createdAt: now } }),
    prisma.assistantMessage.create({ data: { role: "ai", content: JSON.stringify(parsed), createdAt: new Date(now.getTime() + 1) } }),
  ]);
  return parsed;
}
```

> `memberLine(m)` 默认带队长备注——后台助理为内部场景，符合 §4.4「队长备注」字段。`sessionContext` / `listMessages` 不动。

- [ ] **Step 8: 跑测试确认通过**

Run: `pnpm --filter @teampilot/server exec vitest run test/assistant.test.ts test/aiContext.test.ts`
Expected: PASS（含原有「includes only messages within 10 minutes」「ask stores user+ai messages」）。

- [ ] **Step 9: 提交**

```bash
git add packages/server/src/ai/context.ts packages/server/src/assistant/service.ts packages/server/test/aiContext.test.ts packages/server/test/assistant.test.ts
git commit -m "$(printf 'feat(server): 后台助理补足队长规则/未来活动报名/位置概况/出勤统计上下文 (§4)\n\nCo-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>')"
```

---

## Task 5：飞书队员 Bot 上下文补全（§5，公开报名名单）

**Files:**
- Modify: `packages/server/src/ai/memberBot.ts`
- Test: `packages/server/test/memberBot.test.ts`

- [ ] **Step 1: 写失败测试**

在 `packages/server/test/memberBot.test.ts` 的 `describe("answerMemberQuestion", …)` 内追加：

```ts
  it("uses Chinese position label and rich next-activity fields", async () => {
    const m = await prisma.member.create({ data: { name: "小李", primaryPosition: "feeder", level: "intermediate", status: "active", feishuOpenId: "ou_li" } });
    await prisma.activity.create({ data: { name: "周三训练", type: "training", status: "published", location: "体育馆", startTime: new Date(Date.now() + 2 * 86400000), durationMinutes: 100, theme: "二传配合", notes: "带护膝", participants: { create: [{ memberId: m.id, attendanceResponse: "no_response" }] } } });
    await answerMemberQuestion("ou_li", "下一场预计多久", llm, new Date());
    const userArg = (llm.completeJSON as any).mock.calls.at(-1)[1] as string;
    expect(userArg).toContain("主要位置：二传手");
    expect(userArg).toContain("100分钟");
    expect(userArg).toContain("二传配合");
    expect(userArg).toContain("带护膝");
    expect(userArg).toContain("发球手");
  });
  it("lists unresponded activities and public going roster", async () => {
    const me = await prisma.member.create({ data: { name: "我", primaryPosition: "tekong", status: "active", feishuOpenId: "ou_me" } });
    const mate = await prisma.member.create({ data: { name: "队友A", primaryPosition: "striker", status: "active", feishuOpenId: "ou_t" } });
    await prisma.activity.create({ data: { name: "待反馈赛", type: "match", status: "published", location: "x", startTime: new Date(Date.now() + 86400000), participants: { create: [{ memberId: me.id, attendanceResponse: "no_response" }, { memberId: mate.id, attendanceResponse: "going" }] } } });
    await answerMemberQuestion("ou_me", "这次有哪些人去", llm, new Date());
    const userArg = (llm.completeJSON as any).mock.calls.at(-1)[1] as string;
    expect(userArg).toContain("已反馈去:队友A");
    expect(userArg).toContain("你尚未反馈的活动");
    expect(userArg).toContain("待反馈赛");
  });
  it("system prompt allows public signup roster + JSON guard", async () => {
    await prisma.member.create({ data: { name: "甲", primaryPosition: "tekong", status: "active", feishuOpenId: "ou_sys" } });
    await answerMemberQuestion("ou_sys", "随便问", llm, new Date());
    const [system] = (llm.completeJSON as any).mock.calls.at(-1);
    expect(system).toContain("报名名单");
    expect(system).toContain("只返回 JSON 对象");
  });
```

- [ ] **Step 2: 跑测试确认失败**

Run: `pnpm --filter @teampilot/server exec vitest run test/memberBot.test.ts`
Expected: FAIL —— 当前 prompt 用英文位置原值、无预计时长/未反馈区/位置概况/公开声明。

- [ ] **Step 3: 重写 memberBot.ts**

把 `packages/server/src/ai/memberBot.ts` 整体替换为：

```ts
import { prisma } from "../db/client.js";
import type { LLMClient } from "./client.js";
import { zMemberBot } from "./schemas.js";
import { OUTPUT_GUARD, positionBreakdown, futurePublishedActivities, recentEndedDetailed } from "./context.js";
import { positionLabel, levelLabel } from "@teampilot/shared";

const respLabel: Record<string, string> = { going: "去", not_going: "不去", no_response: "未反馈" };

export async function answerMemberQuestion(openId: string, question: string, llm: LLMClient, now: Date): Promise<string> {
  const member = await prisma.member.findUnique({ where: { feishuOpenId: openId } });
  if (!member || member.status !== "active") return "你当前不是活跃队员，请联系队长处理。";

  const activeMembers = await prisma.member.findMany({ where: { status: "active" } });
  const future = await futurePublishedActivities(now);
  const ended = await recentEndedDetailed(now);

  const myResp = (a: typeof future[number]) => a.participants.find((p) => p.memberId === member.id)?.attendanceResponse ?? "no_response";
  const fLine = (a: typeof future[number]) => {
    const going = a.participants.filter((p) => p.attendanceResponse === "going");
    const not = a.participants.filter((p) => p.attendanceResponse === "not_going").length;
    const no = a.participants.filter((p) => p.attendanceResponse === "no_response").length;
    return `${a.name}（${a.type === "training" ? "训练" : "比赛"}） / ${a.startTime.toLocaleString("zh-CN", { timeZone: "Asia/Shanghai" })} / ${a.durationMinutes}分钟 / ${a.location} / 主题:${a.theme ?? "无"} / 注意:${a.notes ?? "无"} / 你的反馈:${respLabel[myResp(a)]} / 去${going.length}·不去${not}·未反馈${no} / 已反馈去:${going.map((p) => p.member.name).join("、") || "无"}`;
  };
  const next = future[0];
  const unResponded = future.filter((a) => myResp(a) === "no_response");
  const publicSummary = ended.map((a) => a.review?.aiSummary ?? a.summary).find((s): s is string => !!s) ?? "暂无";

  const system = "你是球队飞书 Bot，面向已加入队员回答球队公开信息和个人相关信息。你可以回答：已发布活动信息、自己的活动反馈和个人资料、全队公开出勤概况、公开复盘摘要、训练准备建议、近期训练重点和球队公开提醒。请区分系统事实和训练建议；活动时间、地点、报名人数、队员资料等事实必须来自系统数据，系统没有的数据说“暂无”或“请联系队长”。如果是训练准备类问题，可以结合队员位置、活动主题、注意事项和近期公开复盘摘要给出建议。已发布活动的报名名单和反馈状态可以作为全队公开信息回答。输出 JSON：{answer}。" + OUTPUT_GUARD;

  const user = [
    `【当前日期】\n${now.toLocaleString("zh-CN", { timeZone: "Asia/Shanghai" })}`,
    `【你】\n姓名：${member.name}\n主要位置：${positionLabel(member.primaryPosition)}\n备选位置：${member.backupPosition ? positionLabel(member.backupPosition) : "暂无"}\n水平：${levelLabel(member.level) || "暂无"}\n风格：${member.style || "暂无"}`,
    `【未来已发布活动】\n${future.length ? future.map((a) => "- " + fLine(a)).join("\n") : "（暂无）"}`,
    `【下一场活动】\n${next ? fLine(next) : "暂无已发布的下一场活动"}`,
    `【你尚未反馈的活动】\n${unResponded.length ? unResponded.map((a) => `- ${a.name}（${a.type === "training" ? "训练" : "比赛"}） / ${a.startTime.toLocaleString("zh-CN", { timeZone: "Asia/Shanghai" })}`).join("\n") : "（无）"}`,
    `【最近已结束活动】\n${ended.length ? ended.map((a) => `- ${a.startTime.toLocaleDateString("zh-CN", { timeZone: "Asia/Shanghai" })} ${a.name}（${a.type === "training" ? "训练" : "比赛"}） 主题:${a.theme ?? "无"} 摘要:${a.review?.aiSummary ?? a.summary ?? "暂无"}`).join("\n") : "（暂无）"}`,
    `【最近公开复盘摘要】\n${publicSummary}`,
    `【队伍位置概况】\n${positionBreakdown(activeMembers)}`,
    `【问题】\n${question}`,
  ].join("\n\n");

  const raw = await llm.completeJSON(system, user);
  return zMemberBot.parse(JSON.parse(raw)).answer;
}
```

> 不变量：绝不注入 `captainNote`/`rawNotes`/后台助理上下文；摘要恒优先 `aiSummary`（有 aiSummary 时不输出原始 `summary`）。

- [ ] **Step 4: 跑测试确认通过**

Run: `pnpm --filter @teampilot/server exec vitest run test/memberBot.test.ts`
Expected: PASS。原有四条（拒绝非队员、不泄露 captainNote、summary fallback、aiSummary 优先于 summary）+ 新增三条全绿。

- [ ] **Step 5: 全量 server 回归**

Run: `pnpm --filter @teampilot/server test`
Expected: 全部 PASS。

- [ ] **Step 6: 提交**

```bash
git add packages/server/src/ai/memberBot.ts packages/server/test/memberBot.test.ts
git commit -m "$(printf 'feat(server): 飞书队员 Bot 补足本人资料/未来活动/未反馈/公开复盘/位置概况 + 公开报名名单 (§5)\n\nCo-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>')"
```

---

## Task 6：UI 基础控件——章节竖杠 + Radio/Checkbox（UI 问题 1/2/3）

**Files:**
- Modify: `packages/web-admin/src/index.css:96-111`
- Create: `packages/web-admin/src/components/Controls.tsx`
- Create: `packages/web-admin/test/controls.test.tsx`
- Modify: `packages/web-admin/src/pages/ActivityForm.tsx:74-75,91,97`

- [ ] **Step 1: 修复章节竖杠（无单测，CSS）**

把 `packages/web-admin/src/index.css` 的 `.doc-h`（第 96-111 行）替换为：

```css
  .doc-h {
    @apply relative mb-2 mt-6 text-base font-bold text-ink first:mt-0;
    padding-left: 11px;
  }
  .doc-h::before {
    content: "";
    position: absolute;
    left: 0;
    top: 50%;
    transform: translateY(-50%);
    width: 3px;
    height: 14px;
    border-radius: 2px;
    background: theme("colors.brand.DEFAULT");
  }
```

说明：删除 `margin-left: -11px`（竖杠/标题不再左移，竖杠左边缘与正文 `doc-p`（无缩进）左边缘对齐）；竖杠圆角 `999px → 2px`。字体 `text-base`(=14px) + `font-bold`(700) 与间距 `mt-6`(24px)/`mb-2`(8px) 已符合规范。

- [ ] **Step 2: 写控件失败测试**

新建 `packages/web-admin/test/controls.test.tsx`：

```tsx
import { describe, it, expect, vi } from "vitest";
import { render, screen } from "@testing-library/react";
import { userEvent } from "@testing-library/user-event";
import { Radio, Checkbox } from "../src/components/Controls.js";

describe("Controls", () => {
  it("checkbox exposes aria-label, reflects checked, fires onChange", async () => {
    const onChange = vi.fn();
    const { rerender } = render(<Checkbox checked={false} onChange={onChange} ariaLabel="选我" />);
    const box = screen.getByLabelText("选我");
    expect(box).not.toBeChecked();
    await userEvent.click(box);
    expect(onChange).toHaveBeenCalledTimes(1);
    rerender(<Checkbox checked={true} onChange={onChange} ariaLabel="选我" />);
    expect(screen.getByLabelText("选我")).toBeChecked();
  });
  it("radio exposes aria-label, reflects checked, fires onChange", async () => {
    const onChange = vi.fn();
    render(<Radio name="g" checked={false} onChange={onChange} ariaLabel="单选我">文本</Radio>);
    const r = screen.getByLabelText("单选我");
    expect(r).not.toBeChecked();
    await userEvent.click(r);
    expect(onChange).toHaveBeenCalledTimes(1);
    expect(screen.getByText("文本")).toBeInTheDocument();
  });
});
```

- [ ] **Step 3: 跑测试确认失败**

Run: `pnpm --filter @teampilot/web-admin exec vitest run test/controls.test.tsx`
Expected: FAIL —— `Controls.tsx` 不存在。

- [ ] **Step 4: 实现 Controls.tsx**

新建 `packages/web-admin/src/components/Controls.tsx`：

```tsx
import { Check } from "./icons.js";

type RadioProps = { checked: boolean; onChange: () => void; name: string; ariaLabel: string; children?: React.ReactNode };
export function Radio({ checked, onChange, name, ariaLabel, children }: RadioProps) {
  return (
    <label className="inline-flex cursor-pointer items-center gap-2 text-base text-ink">
      <span className="relative inline-flex h-[18px] w-[18px] items-center justify-center">
        <input type="radio" name={name} aria-label={ariaLabel} checked={checked} onChange={onChange} className="peer sr-only" />
        <span className="absolute inset-0 rounded-full border-[1.5px] border-ink-weak bg-white transition-colors duration-150 peer-hover:border-brand peer-checked:border-2 peer-checked:border-brand" />
        <span className="absolute left-1/2 top-1/2 h-2 w-2 -translate-x-1/2 -translate-y-1/2 rounded-full bg-brand opacity-0 transition-opacity duration-150 peer-checked:opacity-100" />
      </span>
      {children}
    </label>
  );
}

type CheckboxProps = { checked: boolean; onChange: () => void; ariaLabel: string; className?: string };
export function Checkbox({ checked, onChange, ariaLabel, className = "" }: CheckboxProps) {
  return (
    <label className={`relative inline-flex h-[18px] w-[18px] cursor-pointer items-center justify-center ${className}`}>
      <input type="checkbox" aria-label={ariaLabel} checked={checked} onChange={onChange} className="peer sr-only" />
      <span className="absolute inset-0 rounded border-[1.5px] border-ink-weak bg-white transition-colors duration-150 peer-hover:border-brand peer-checked:border-brand peer-checked:bg-brand" />
      <Check size={12} className="relative text-white opacity-0 transition-opacity duration-150 peer-checked:opacity-100" />
    </label>
  );
}
```

说明：`peer sr-only` 隐藏原生 input 但保留可访问性与 `checked` 状态；外圈/圆点/对勾都是 input 的**同级**元素，`peer-checked:` 才能生效。复选框选中时方框变 brand 底、白色对勾显现；单选选中时外圈转 brand 并显出 brand 圆点；hover 边框转 brand。

- [ ] **Step 5: 跑控件测试确认通过**

Run: `pnpm --filter @teampilot/web-admin exec vitest run test/controls.test.tsx`
Expected: PASS。

- [ ] **Step 6: 在 ActivityForm 替换原生控件**

在 `packages/web-admin/src/pages/ActivityForm.tsx`：

顶部 import（第 1-6 行附近）增加：

```tsx
import { Radio, Checkbox } from "../components/Controls.js";
```

活动类型两个 `<label>…<input type="radio" …/>…</label>`（第 74-75 行）替换为：

```tsx
            <Radio name="type" ariaLabel="类型-训练" checked={type === "training"} onChange={() => setType("training")}>训练</Radio>
            <Radio name="type" ariaLabel="类型-比赛" checked={type === "match"} onChange={() => setType("match")}>比赛</Radio>
```

表头全选 checkbox（第 91 行）替换为：

```tsx
            <th className="w-12"><Checkbox ariaLabel="全选" checked={allSelected} onChange={toggleAll} /></th>
```

每行参加 checkbox（第 97 行）替换为：

```tsx
                <td><Checkbox ariaLabel={`参加-${m.name}`} checked={selected.has(m.id)} onChange={() => toggle(m.id)} /></td>
```

- [ ] **Step 7: 跑 ActivityForm 测试确认通过**

Run: `pnpm --filter @teampilot/web-admin exec vitest run test/activityForm.test.tsx`
Expected: PASS（`getByLabelText("参加-甲")).toBeChecked()`、`getByLabelText("类型-训练")` 点击仍有效——`aria-label`/`checked`/`onChange` 保留在原生 input 上）。

- [ ] **Step 8: 提交**

```bash
git add packages/web-admin/src/index.css packages/web-admin/src/components/Controls.tsx packages/web-admin/test/controls.test.tsx packages/web-admin/src/pages/ActivityForm.tsx
git commit -m "$(printf 'feat(web-admin): 章节竖杠对齐修复 + Pine 自定义 Radio/Checkbox 控件 (UI 1/2/3)\n\nCo-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>')"
```

---

## Task 7：移动端响应式（核心 + D 节，UI 问题 5）

> **范围说明（据实裁剪）：** UI 文档 D 节的「出勤统计卡片区四列→两列」「出勤队员列表多列→单列」在本代码库**不存在**——出勤为 `AttendanceTab` 表格，已通过 `overflow-x-auto + min-w` 在窄屏横向滚动，无需改。D 节实际落地项为：活动详情 Banner 纵向化、AI 聊天容器移动端透明/全高、表单操作按钮纵向全宽。断点用 Tailwind `md`(768px，桌面用 `md:` 前缀，移动为 base/`max-md:`)。

**Files:**
- Modify: `packages/web-admin/src/components/icons.tsx`（新增 Clock/MapPin）
- Create: `packages/web-admin/src/components/MobileNav.tsx`
- Modify: `packages/web-admin/src/components/Layout.tsx`
- Modify: `packages/web-admin/src/pages/Activities.tsx`
- Modify: `packages/web-admin/src/pages/ActivityDetail.tsx`
- Modify: `packages/web-admin/src/pages/Assistant.tsx`
- Modify: `packages/web-admin/src/pages/ActivityForm.tsx`
- Test: `packages/web-admin/test/activities.test.tsx`、`packages/web-admin/test/layout.test.tsx`

- [ ] **Step 1: 处理 activities 测试的「双 DOM」陷阱（先改测试）**

移动端卡片与桌面表格会同时渲染于 jsdom（无媒体查询），`screen.getByText` 会命中多个同名文本。把 `packages/web-admin/test/activities.test.tsx` 现有 `it("renders rows …")` 用例改为限定在桌面表格内查询：

```tsx
  it("renders rows with type/status labels + derived columns and a 创建活动 link", async () => {
    render(<MemoryRouter><Activities /></MemoryRouter>);
    const table = within(await screen.findByRole("table"));
    expect(table.getByText("周日训练")).toBeInTheDocument();
    expect(table.getByText("训练")).toBeInTheDocument();
    expect(table.getByText("已发布")).toBeInTheDocument();
    expect(table.getByText("3 去 ｜ 1 不去 ｜ 2 未反馈")).toBeInTheDocument();
    expect(screen.getByRole("link", { name: "创建活动" })).toBeInTheDocument();
  });
```

并把第 2 行 import 改为引入 `within`：

```tsx
import { render, screen, within } from "@testing-library/react";
```

> Task 1 新增的「shows 编辑 only for draft」用例不受影响：卡片是 `<Link>` 但其可访问名为整段卡片文本，`getAllByRole("link",{name:"编辑"/"详情"})` 仍只命中表格内的文字链接。

- [ ] **Step 2: 跑测试确认仍通过（基线）**

Run: `pnpm --filter @teampilot/web-admin exec vitest run test/activities.test.tsx`
Expected: PASS（卡片尚未加，within(table) 查询正常）。

- [ ] **Step 3: 新增 Clock / MapPin 图标**

在 `packages/web-admin/src/components/icons.tsx` 末尾（`Pencil` 之后）追加：

```tsx
export const Clock = ({ className, size }: P) => (
  <svg {...base(size)} className={className}><circle cx="12" cy="12" r="9" /><polyline points="12 7 12 12 15 14" /></svg>
);
export const MapPin = ({ className, size }: P) => (
  <svg {...base(size)} className={className}><path d="M21 10c0 6-9 12-9 12s-9-6-9-12a9 9 0 0 1 18 0z" /><circle cx="12" cy="10" r="3" /></svg>
);
```

- [ ] **Step 4: 新建底部 Tab 栏 MobileNav.tsx**

新建 `packages/web-admin/src/components/MobileNav.tsx`：

```tsx
import { NavLink } from "react-router-dom";
import { Users, Calendar, Sparkle, Gear } from "./icons.js";

const ITEMS = [
  { to: "/members", label: "队员", Icon: Users },
  { to: "/activities", label: "活动", Icon: Calendar },
  { to: "/assistant", label: "助手", Icon: Sparkle },
  { to: "/settings", label: "设置", Icon: Gear },
];

export default function MobileNav() {
  return (
    <nav className="fixed inset-x-0 bottom-0 z-40 flex h-16 items-center justify-around border-t border-line bg-surface-card px-2.5 backdrop-blur-xl md:hidden">
      {ITEMS.map(({ to, label, Icon }) => (
        <NavLink
          key={to}
          to={to}
          className={({ isActive }) =>
            `flex flex-1 flex-col items-center justify-center gap-1 text-[10px] transition-colors ${isActive ? "text-brand" : "text-ink-soft"}`
          }
        >
          <Icon size={20} />
          {label}
        </NavLink>
      ))}
    </nav>
  );
}
```

> 标签用短词（队员/活动/助手/设置），与侧栏长标签（队员管理…）不冲突，`layout.test.tsx` 现有 `getByText("队员管理")` 等仍唯一命中。

- [ ] **Step 5: Layout 接入 MobileNav + 侧栏隐藏 + 主区留白**

在 `packages/web-admin/src/components/Layout.tsx`：

第 1-3 行 import 区增加：

```tsx
import MobileNav from "./MobileNav.js";
```

侧栏 `<aside …>`（第 18 行）className 由 `flex w-[224px] shrink-0 flex-col px-4 py-5` 改为：

```tsx
      <aside className="hidden w-[224px] shrink-0 flex-col px-4 py-5 md:flex">
```

主区 `<main …>`（第 73 行）className 由 `min-w-0 flex-1 py-4 pr-4` 改为：

```tsx
      <main className="min-w-0 flex-1 py-4 pl-4 pr-4 pb-20 md:pl-0 md:pb-4">
```

在 `</main>` 之后、根 `</div>` 之前插入：

```tsx
      <MobileNav />
```

> Tailwind v3 工具排序保证 `pb-20`/`md:pb-4` 覆盖 `py-4` 的下内边距、`md:pl-0` 覆盖 `pl-4`。移动端无侧栏时左右各 16px、底部 80px 给 Tab 栏留白。

- [ ] **Step 6: Activities 增移动端卡片列表**

在 `packages/web-admin/src/pages/Activities.tsx`：

第 6 行 icons import 增加 `Clock, MapPin`：

```tsx
import { Plus, Clock, MapPin } from "../components/icons.js";
```

把表格容器 `<div className="table-wrap overflow-x-auto">`（第 37 行）改为：

```tsx
      <div className="table-wrap hidden overflow-x-auto md:block">
```

在该表格容器 `</div>`（第 60 行）之后插入移动端卡片列表：

```tsx
      <div className="flex flex-col gap-4 md:hidden">
        {rows.map((a) => (
          <Link key={a.id} to={`/activities/${a.id}`} className="flex flex-col gap-3 rounded-lg border border-line bg-surface p-4">
            <div className="flex items-start justify-between gap-2">
              <span className="text-base font-semibold text-ink">{a.name}</span>
              <Badge tone="neutral">{typeLabel(a.type)}</Badge>
            </div>
            <div className="flex flex-col gap-1.5 border-b border-line pb-3 text-sm text-ink-soft">
              <span className="flex items-center gap-1.5"><Clock size={14} className="text-ink-weak" />{new Date(a.startTime).toLocaleString("zh-CN")}</span>
              <span className="flex items-center gap-1.5"><MapPin size={14} className="text-ink-weak" />{a.location}</span>
            </div>
            <div className="flex items-center justify-between text-sm">
              <Badge tone={statusTone[a.status] ?? "neutral"}>{statusLabel[a.status]}</Badge>
              <span className="text-xs text-ink-soft">{a.attendanceSummary}</span>
            </div>
          </Link>
        ))}
        {rows.length === 0 && <p className="py-8 text-center text-sm text-ink-weak">暂无活动，点击右上角「创建活动」开始</p>}
      </div>
```

> 中间态（768–1024px）横向滚动已由表格容器 `overflow-x-auto` + `table-pine min-w-[940px]` 满足，无需额外断点。

- [ ] **Step 7: 详情 Banner / 聊天容器 / 表单按钮 移动端适配（D 节）**

`packages/web-admin/src/pages/ActivityDetail.tsx` 标题行（第 36 行）：

```tsx
      <div className="mb-5 flex flex-col items-start gap-3 md:flex-row md:items-center md:justify-between md:gap-4">
```

其下操作按钮容器 `<div className="flex gap-2">`（第 38 行）：

```tsx
        <div className="flex w-full gap-2 md:w-auto">
```

`packages/web-admin/src/pages/Assistant.tsx` 外层容器（第 36 行）：

```tsx
    <div className="mx-auto flex h-[calc(100vh-176px)] max-w-chat flex-col max-md:h-[calc(100vh-132px)]">
```

其内层卡片容器（第 39 行）：

```tsx
      <div className="flex min-h-0 flex-1 flex-col overflow-hidden rounded-lg border border-line bg-surface-card max-md:rounded-none max-md:border-0 max-md:bg-transparent">
```

`packages/web-admin/src/pages/ActivityForm.tsx` 底部操作按钮容器 `<div className="mt-6 flex gap-2">`（第 112 行）：

```tsx
      <div className="mt-6 flex flex-col gap-2 md:flex-row">
```

并把这三个按钮加 `w-full md:w-auto`（第 113-115 行）：

```tsx
        <button className="btn-ghost w-full md:w-auto" onClick={() => nav(-1)}>取消</button>
        <button className="btn-secondary w-full md:w-auto" onClick={() => void saveDraft()}>保存草稿</button>
        <button className="btn-primary w-full md:w-auto" onClick={onPublishClick}>发布</button>
```

- [ ] **Step 8: 给 layout.test 增补移动端 Tab 断言**

在 `packages/web-admin/test/layout.test.tsx` 的 `describe("Layout", …)` 内追加：

```tsx
  it("renders mobile bottom nav tabs", async () => {
    render(<AuthProvider><MemoryRouter><Layout><div>内容</div></Layout></MemoryRouter></AuthProvider>);
    expect(await screen.findByText("助手")).toBeInTheDocument();
    expect(screen.getByText("活动")).toBeInTheDocument();
  });
```

> 「助手」「活动」为 MobileNav 短标签，与侧栏「AI 队长助理」「活动管理」不冲突，断言唯一命中底部 Tab。

- [ ] **Step 9: 跑前端全量回归**

Run: `pnpm --filter @teampilot/web-admin test`
Expected: 全部 PASS（activities/assistant/activityForm/layout/controls/members/attendance/reviewTab/login/toast/notifyStatus）。

- [ ] **Step 10: 提交**

```bash
git add packages/web-admin/src/components/icons.tsx packages/web-admin/src/components/MobileNav.tsx packages/web-admin/src/components/Layout.tsx packages/web-admin/src/pages/Activities.tsx packages/web-admin/src/pages/ActivityDetail.tsx packages/web-admin/src/pages/Assistant.tsx packages/web-admin/src/pages/ActivityForm.tsx packages/web-admin/test/activities.test.tsx packages/web-admin/test/layout.test.tsx
git commit -m "$(printf 'feat(web-admin): 移动端响应式——底部 Tab 栏/活动卡片列表/详情 Banner/聊天容器/表单按钮 (UI 5)\n\nCo-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>')"
```

---

## 最终验收

- [ ] **全量回归**

```bash
pnpm --filter @teampilot/server test
pnpm --filter @teampilot/web-admin test
```

Expected: 两包测试全绿。

- [ ] **构建校验（可选）**

```bash
pnpm --filter @teampilot/web-admin exec tsc --noEmit
pnpm --filter @teampilot/server exec tsc -p tsconfig.json --noEmit
```

Expected: 无类型错误。

- [ ] **人工复测**（按 spec §6.2）：草稿/非草稿编辑入口 → 助理立即显示用户消息 → 各 AI 场景 prompt 输入 → 桌面/移动端（≤768px / 768–1024px）视觉与导航。

## 自检：spec 覆盖

- §2 OUTPUT_GUARD → T3 ✓
- §3.1–3.4 四生成场景 → T3 ✓
- §4 后台助理 system + 五类输入（队长规则/位置概况/未来活动/近2月摘要/出勤统计）→ T4 ✓
- §5 飞书 Bot system + 六类输入 + 公开报名名单 → T5 ✓
- UI 问题 1 竖杠 → T6 ✓；问题 2/3 控件 → T6 ✓；问题 4 机器人 → 决策不改（不在范围）；问题 5 移动端 → T7 ✓（D 节据实裁剪已说明）
- TP2-ACT-01 → T1 ✓；TP2-ASST-01 → T2 ✓
- §6.1 测试覆盖 → T1/T2/T3/T4/T5 测试步骤 ✓
